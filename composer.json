{
    "name": "lite-annotate/lite-annotate",
    "description": "Lightweight annotation application for text classification tasks.",
    "type": "project",
    "require": {
        "php": "^8.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^12.2"
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml.dist"
    }
}